{
  "name": "EventSystem_user_front-end",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "vue-tsc --build --force",
    "lint-staged": "lint-staged",
    "prepare": "husky install"
  },
  "dependencies": {
    "element-plus": "^2.10.0",
    "vue": "^3.5.26"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.3",
    "@vue/tsconfig": "^0.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.5.3",
    "vite": "^7.3.0",
    "vite-plugin-vue-devtools": "^8.0.5"
  },
  "lint-staged": {
    "*.{js,ts,vue,css,scss,md,json}": [
      "prettier --write"
    ]
  }
}
